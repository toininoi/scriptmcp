name: Release ScriptMCP.Console

on:
  push:
    branches:
      - main
    paths:
      - "ScriptMCP.Console/**"
      - "ScriptMCP.Library/**"
      - "ScriptMCP.Extension/**"
      - "ScriptMCP.Plugin/**"
      - ".github/workflows/release-console.yml"
  workflow_dispatch:
    inputs:
      custom_version:
        description: Optional explicit version number to release, for example 2.3.4
        required: false
        type: string

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  version:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.version.outputs.tag }}
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Compute Version And Tag
        id: version
        shell: bash
        env:
          CUSTOM_VERSION: ${{ inputs.custom_version }}
        run: |
          set -euo pipefail

          if [[ -n "${CUSTOM_VERSION:-}" ]]; then
            if [[ ! "$CUSTOM_VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
              echo "custom_version must match MAJOR.MINOR.PATCH, for example 2.3.4" >&2
              exit 1
            fi

            custom_tag="scriptmcp-v${CUSTOM_VERSION}"
            existing_custom_tag_commit="$(git rev-list -n 1 "$custom_tag" 2>/dev/null || true)"
            if [[ -n "$existing_custom_tag_commit" && "$existing_custom_tag_commit" != "${GITHUB_SHA}" ]]; then
              echo "Tag $custom_tag already exists on commit $existing_custom_tag_commit and cannot be reused for ${GITHUB_SHA}." >&2
              exit 1
            fi

            echo "tag=$custom_tag" >> "$GITHUB_OUTPUT"
            echo "version=$CUSTOM_VERSION" >> "$GITHUB_OUTPUT"

            if [[ -z "$existing_custom_tag_commit" ]]; then
              git config user.name "github-actions[bot]"
              git config user.email "github-actions[bot]@users.noreply.github.com"
              git tag "$custom_tag"
              git push origin "$custom_tag"
            fi

            exit 0
          fi

          # If this commit already has a scriptmcp-v* tag, reuse it.
          existing_tag="$(git tag --points-at HEAD | grep -E '^scriptmcp-v[0-9]+\.[0-9]+\.[0-9]+$' | head -n 1 || true)"
          if [[ -n "$existing_tag" ]]; then
            echo "tag=$existing_tag" >> "$GITHUB_OUTPUT"
            echo "version=${existing_tag#scriptmcp-v}" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Find latest scriptmcp-v* tag, default to 1.0.0 if none exists.
          latest_tag="$(git tag --list 'scriptmcp-v*' --sort=-v:refname | head -n 1 || true)"
          if [[ -z "$latest_tag" ]]; then
            major=1
            minor=0
            patch=0
          else
            version="${latest_tag#scriptmcp-v}"
            IFS='.' read -r major minor patch <<< "$version"
          fi

          # Increment patch; roll over patch 0-9 into minor.
          patch=$((patch + 1))
          if [[ "$patch" -ge 10 ]]; then
            patch=0
            minor=$((minor + 1))
          fi

          next_version="${major}.${minor}.${patch}"
          next_tag="scriptmcp-v${next_version}"
          echo "tag=$next_tag" >> "$GITHUB_OUTPUT"
          echo "version=$next_version" >> "$GITHUB_OUTPUT"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "$next_tag"
          git push origin "$next_tag"

  cleanup-old-release-state:
    runs-on: ubuntu-latest
    needs: version
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Delete Older Releases Tags And Attestations
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          CURRENT_TAG: ${{ needs.version.outputs.tag }}
          REPOSITORY: ${{ github.repository }}
          REPOSITORY_OWNER: ${{ github.repository_owner }}
          REPOSITORY_OWNER_TYPE: ${{ github.event.repository.owner.type }}
        run: |
          set -euo pipefail

          case "$REPOSITORY_OWNER_TYPE" in
            Organization)
              attestation_base="orgs/$REPOSITORY_OWNER/attestations"
              ;;
            User)
              attestation_base="users/$REPOSITORY_OWNER/attestations"
              ;;
            *)
              echo "Unsupported repository owner type: $REPOSITORY_OWNER_TYPE" >&2
              exit 1
              ;;
          esac

          digests_file="$(mktemp)"
          tags_file="$(mktemp)"
          payload_file="$(mktemp)"
          trap 'rm -f "$digests_file" "$tags_file" "$payload_file"' EXIT

          collect_release_state() {
            local tag="$1"

            if gh release view "$tag" --repo "$REPOSITORY" >/dev/null 2>&1; then
              release_json="$(gh api "/repos/$REPOSITORY/releases/tags/$tag" 2>/dev/null || true)"
              if [[ -n "$release_json" ]]; then
                mapfile -t digests < <(
                  printf '%s' "$release_json" |
                    jq -r '.assets[]?.digest // empty' |
                    grep '^sha256:' |
                    sort -u
                )

                if [[ ${#digests[@]} -gt 0 ]]; then
                  printf '%s\n' "${digests[@]}" >> "$digests_file"
                fi
              fi
            fi

            printf '%s\n' "$tag" >> "$tags_file"
          }

          mapfile -t old_tags < <(git tag --list 'scriptmcp-v*' | grep -vx "$CURRENT_TAG" || true)
          for tag in "${old_tags[@]}"; do
            [[ -n "$tag" ]] || continue
            collect_release_state "$tag"
          done

          if [[ -s "$digests_file" ]]; then
            jq -Rn '
              [inputs | select(length > 0)] | unique | {subject_digests: .}
            ' < "$digests_file" > "$payload_file"

            gh api \
              --method POST \
              -H "Accept: application/vnd.github+json" \
              -H "X-GitHub-Api-Version: 2022-11-28" \
              "/$attestation_base/delete-request" \
              --input "$payload_file" || echo "Warning: attestation deletion failed, continuing"
          fi

          if [[ -s "$tags_file" ]]; then
            mapfile -t tags_to_delete < <(sort -u "$tags_file")
            for tag in "${tags_to_delete[@]}"; do
              [[ -n "$tag" ]] || continue
              gh release delete "$tag" --repo "$REPOSITORY" -y >/dev/null 2>&1 || true
              git push origin ":refs/tags/$tag" >/dev/null 2>&1 || true
              git tag -d "$tag" >/dev/null 2>&1 || true
            done
          fi

  create-release:
    runs-on: ubuntu-latest
    needs: [version, cleanup-old-release-state]
    steps:
      - name: Create Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "${{ needs.version.outputs.tag }}" --repo "${{ github.repository }}" >/dev/null 2>&1; then
            exit 0
          fi

          gh release create "${{ needs.version.outputs.tag }}" \
            --repo "${{ github.repository }}" \
            --title "ScriptMCP Plugin ${{ needs.version.outputs.version }}" \
            --generate-notes

  publish:
    runs-on: ubuntu-latest
    needs: [version, create-release]
    strategy:
      fail-fast: false
      matrix:
        rid: [win-x64, linux-x64, osx-arm64]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          # Keep this aligned with global.json and the net9.0 target frameworks.
          dotnet-version: "9.0.x"

      - name: Publish
        run: |
          SELF_CONTAINED_FOLDER="ScriptMCP ${{ needs.version.outputs.version }} ${{ matrix.rid }}"

          dotnet publish ScriptMCP.Console/ScriptMCP.Console.csproj \
            -c Release \
            -r ${{ matrix.rid }} \
            --self-contained true \
            -p:PublishSingleFile=true \
            -p:IncludeNativeLibrariesForSelfExtract=true \
            -p:PublishTrimmed=false \
            -o "artifacts/$SELF_CONTAINED_FOLDER"

          if [[ "${{ matrix.rid }}" != win-* ]]; then
            chmod +x "artifacts/$SELF_CONTAINED_FOLDER/scriptmcp"
          fi

      - name: Package MCPB Extension
        run: |
          SELF_CONTAINED_FOLDER="ScriptMCP ${{ needs.version.outputs.version }} ${{ matrix.rid }}"
          MCPB_STAGE="artifacts/mcpb-stage"
          mkdir -p "$MCPB_STAGE/server"

          # Copy the self-contained binary into the server folder
          if [[ "${{ matrix.rid }}" == win-* ]]; then
            BINARY_NAME="scriptmcp.exe"
            ENTRY_POINT="server/scriptmcp.exe"
          else
            BINARY_NAME="scriptmcp"
            ENTRY_POINT="server/scriptmcp"
          fi
          cp "artifacts/$SELF_CONTAINED_FOLDER/$BINARY_NAME" "$MCPB_STAGE/server/"

          # Generate a platform-specific manifest.json with the correct entry_point and version
          jq --arg ep "$ENTRY_POINT" --arg ver "${{ needs.version.outputs.version }}" \
            '.server.entry_point = $ep | .server.mcp_config.command = ("${__dirname}/" + $ep) | .version = $ver' \
            ScriptMCP.Extension/manifest.json > "$MCPB_STAGE/manifest.json"

          cd "$MCPB_STAGE"
          zip -r "../scriptmcp-${{ matrix.rid }}.mcpb" .

      - name: Package Claude Code Plugin
        if: matrix.rid == 'win-x64'
        run: |
          PLUGIN_STAGE="artifacts/plugin-stage"

          # Copy plugin structure
          mkdir -p "$PLUGIN_STAGE"
          cp -r ScriptMCP.Plugin/.claude-plugin "$PLUGIN_STAGE/.claude-plugin"
          cp -r ScriptMCP.Plugin/commands "$PLUGIN_STAGE/commands"
          cp -r ScriptMCP.Plugin/hooks "$PLUGIN_STAGE/hooks"
          cp -r ScriptMCP.Plugin/skills "$PLUGIN_STAGE/skills"
          mkdir -p "$PLUGIN_STAGE/server"

          # Update plugin.json with the version
          jq --arg ver "${{ needs.version.outputs.version }}" \
            '.version = $ver' \
            "$PLUGIN_STAGE/.claude-plugin/plugin.json" > "$PLUGIN_STAGE/.claude-plugin/plugin.json.tmp"
          mv "$PLUGIN_STAGE/.claude-plugin/plugin.json.tmp" "$PLUGIN_STAGE/.claude-plugin/plugin.json"

          cd "$PLUGIN_STAGE"
          zip -r "../scriptmcp-plugin.zip" .

      - name: Attest MCPB Build Provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: "artifacts/scriptmcp-${{ matrix.rid }}.mcpb"

      - name: Attest Plugin Build Provenance
        if: matrix.rid == 'win-x64'
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: "artifacts/scriptmcp-plugin.zip"

      - name: Upload Assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          ASSETS=("artifacts/scriptmcp-${{ matrix.rid }}.mcpb")
          if [[ "${{ matrix.rid }}" == "win-x64" ]]; then
            ASSETS+=("artifacts/scriptmcp-plugin.zip")
          fi
          gh release upload "${{ needs.version.outputs.tag }}" \
            "${ASSETS[@]}" \
            --repo "${{ github.repository }}" \
            --clobber
